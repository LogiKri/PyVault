# LOC = local SQLite file in ./data/pyvault.db
# Anything else = MySQL server, using the DB_* settings below
LOC_OR_SERVER=LOC

# Only needed when LOC_OR_SERVER is not "LOC"
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=pyvault
